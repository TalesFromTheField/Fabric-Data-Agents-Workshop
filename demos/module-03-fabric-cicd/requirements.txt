# Module 03 CI/CD demo dependencies.
#
# Requires Python 3.10 or newer. fabric-cicd 1.3.0 advertises Python 3.9 support
# but fails to import on 3.9 (it uses `X | None` annotations that 3.9 evaluates
# at runtime). macOS ships 3.9 as the system Python, so create a virtual
# environment with a newer interpreter.

fabric-cicd>=1.3.0
azure-identity>=1.25.0

# deploy.py reads config.yml and parameter.yml directly for its `inspect`
# command. fabric-cicd depends on PyYAML too, but declare it explicitly rather
# than relying on a transitive dependency.
pyyaml>=6.0.2

# Optional - only needed for the Fabric CLI variant in ./fabric-cli/.
# Install separately when you want it:
#   pip install ms-fabric-cli
